import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Quote Generator - Find Inspiring Quotes",
  description: "Generate inspiring quotes on any topic. Discover wisdom from great minds throughout history.",
  keywords: ["quotes", "inspiration", "motivation", "wisdom", "success", "life"],
  authors: [{ name: "Quote Generator Team" }],
  creator: "Nexium Assignment 1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
